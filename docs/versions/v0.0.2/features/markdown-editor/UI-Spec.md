# SlideNote Markdown 编辑器 UI 设计文档

> **版本**: v0.0.2
> **设计师**: Claude
> **创建日期**: 2025-01-13

---

## 设计理念

> **"格式在幕后，内容在前台"**

Markdown 符号应该像舞台工作人员一样，默默支持内容展示，但不抢走内容的注意力。

### 核心原则

| 原则 | 解释 |
|------|------|
| **半透明提示** | Markdown 符号以 30% 透明度显示，"存在但不干扰" |
| **即时反馈** | 输入完成后立即渲染，无等待感 |
| **渐进增强** | 不熟悉 Markdown 的用户也能正常使用 |
| **视觉一致性** | 与现有设计语言保持统一 |

---

## 色彩系统扩展

### Markdown 语法专用色彩

```css
/* Markdown 分隔符（半透明） */
--color-md-delimiter: rgba(102, 102, 102, 0.3);

/* Markdown 代码背景 */
--color-md-code-bg: #f8f9fa;
--color-md-code-text: #e83e8c;

/* Markdown 链接 */
--color-md-link: #0066cc;
--color-md-link-hover: #0052a3;

/* Markdown 引用 */
--color-md-quote-bg: #f8f9fa;
--color-md-quote-border: #dee2e6;

/* Markdown 分割线 */
--color-md-hr: #e5e5e5;
```

---

## 编辑器布局设计

### 整体布局

```
┌─────────────────────────────────────────────────────────┐
│  SlideNote                               ⋮             │
├────────────┬────────────────────────────────────────────┤
│            │  常用账号                            [⋮]   │
│            │  ────────────────────────────────────────   │
│            │                                             │
│  笔记列表  │  # 重要事项                                │
│            │  ────────────────────────────────────────   │
│            │                                             │
│            │  ## 待办事项                                │
│            │  - [ ] 完成项目报告                        │
│            │  - [x] 联系客户                            │
│            │                                             │
│            │  ## 备注                                    │
│            │  记得给 **张三** 发邮件，主题是 `Q1规划`   │
│            │                                             │
│            │  > "行动是治愈恐惧的良药"                   │
│            │                                             │
│            │  ────────────────────────────────────────   │
│            │                                             │
│            │  [✓ 已保存]              最后编辑 3 分钟前  │
└────────────┴────────────────────────────────────────────┘
```

### 编辑器头部设计

```
┌───────────────────────────────────────────────────────────┐
│  常用账号                                          [⋮]    │
└───────────────────────────────────────────────────────────┘

高度: 48px
内边距: 12px 16px 8px 16px
边框底部: 1px solid var(--color-border)
```

**标题输入框**：
- 字号: 18px
- 字重: 600
- 无边框，透明背景
- 占位符颜色: #bbbbbb

**更多按钮 [⋮]**：
- 位置: 右上角
- 尺寸: 32x32px
- 图标: 三个竖点 (16px)
- 悬停: 背景变为 #f5f5f5

### 更多菜单设计

```
┌────────────────────────────┐
│ 📋 复制为 Markdown          │
│ ─────────────────────────  │
│ ℹ️  Markdown 语法提示       │
└────────────────────────────┘

宽度: 160px
圆角: 8px
阴影: 0 8px 24px rgba(0,0,0,0.12)
位置: 右对齐，按钮下方 4px
```

**菜单项样式**：
- 高度: 36px
- 内边距: 8px 12px
- 图标宽度: 16px，左对齐
- 悬停背景: #f5f5f5
- 字号: 13px

---

## Markdown 渲染样式

### 1. 标题样式

```
# H1 一级标题
────────────────────────────────────────
字号: 20px / 字重: 600 / 上边距: 16px / 下边距: 8px
分隔符 (#): 12px / 30% 透明度

## H2 二级标题
────────────────────────────────────────
字号: 16px / 字重: 600 / 上边距: 12px / 下边距: 6px
分隔符 (##): 12px / 30% 透明度

### H3 三级标题
────────────────────────────────────────
字号: 14px / 字重: 600 / 上边距: 8px / 下边距: 4px
分隔符 (###): 12px / 30% 透明度
```

### 2. 粗体与斜体

```
**粗体文本** → 粗体文本
────────────────────────────────────────
字重: 600
分隔符 (**): 30% 透明度，user-select: none

*斜体文本* → 斜体文本
────────────────────────────────────────
字体样式: italic
分隔符 (*): 30% 透明度，user-select: none

***粗斜体*** → 粗斜体文本
────────────────────────────────────────
字重: 600 + italic
```

### 3. 链接样式

```
[文本](https://example.com)
────────────────────────────────────────
[文本] — 颜色: #0066cc，下划线，悬停时加深
(url) — 30% 透明度显示
```

**链接交互状态**：
- 默认: 蓝色 (#0066cc)，下划线
- 悬停: 深蓝色 (#0052a3)
- 点击: 深蓝色 + 活跃状态

### 4. 行内代码

```
`代码`
────────────────────────────────────────
背景: #f8f9fa
圆角: 4px
内边距: 2px 6px
字号: 12px (比正文小 1px)
字族: SF Mono, Consolas, monospace
颜色: #e83e8c
分隔符 (`): 30% 透明度
```

### 5. 代码块

```
```javascript
function hello() {
  console.log("Hello World");
}
```
────────────────────────────────────────
背景: #f8f9fa
圆角: 6px
内边距: 12px
字号: 12px
字族: SF Mono, Consolas, monospace
行高: 1.6
上下外边距: 8px
```

### 6. 列表样式

```
- 无序列表项
────────────────────────────────────────
列表符号: • (Unicode U+2022)
符号颜色: 30% 透明度
符号与文字间距: 6px
左侧缩进: 18px
行间距: 4px

1. 有序列表项
────────────────────────────────────────
数字颜色: 30% 透明度
点号后空格: 4px
左侧缩进: 18px
```

### 7. 引用样式

```
> 这是一段引用文字
────────────────────────────────────────
左侧边框: 3px solid #dee2e6
背景: #f8f9fa
内边距: 8px 12px
左侧缩进: 0
上下外边距: 8px
字号: 13px
颜色: #666666 (次要文字色)
分隔符 (>): 30% 透明度
```

### 8. 分割线

```
---
────────────────────────────────────────
高度: 1px
颜色: #e5e5e5
上下外边距: 16px
分隔符 (---): 完全透明
```

---

## 语法提示弹窗设计

```
┌─────────────────────────────────────────────────────────┐
│  Markdown 语法提示                                 [×]  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  格式        语法          效果                         │
│  ────────────────────────────────────────────────────   │
│  粗体        **文本**      **粗体**                     │
│  斜体        *文本*        *斜体*                       │
│  行内代码    `代码`        `代码`                       │
│  链接        [文本](网址)  [链接](https://)             │
│  标题        # 标题       # 大号标题                   │
│  列表        - 项目        • 项目                      │
│  引用        > 引用       > 带边框引用                 │
│                                                         │
│  💡 提示：分隔符会自动变淡，不会影响阅读                │
│                                                         │
└─────────────────────────────────────────────────────────┘

宽度: 320px
圆角: 8px
遮罩: rgba(0,0,0,0.4)
```

**表格样式**：
- 表头字号: 11px，颜色: #999999
- 语法列字号: 12px，等宽字体，背景 #f5f5f5
- 效果列字号: 13px，实时渲染效果

---

## 复制成功提示设计

```
┌─────────────────────────────────┐
│  ✓ 已复制 Markdown 源码到剪贴板  │
└─────────────────────────────────┘

位置: 编辑器右上角按钮下方
高度: 36px
内边距: 0 16px
圆角: 6px
背景: #1a1a1a
文字: 白色
动画: 淡入 150ms，停留 2000ms，淡出 150ms
```

---

## 焦点与选中状态

### 编辑器焦点

```
编辑器获得焦点时：
- 无边框变化（保持简洁）
- 光标颜色: #0066cc
- 光标宽度: 2px
```

### 文本选中

```
选中文本时：
- 背景色: rgba(0, 102, 204, 0.15)
- 文字颜色: 保持原色
```

---

## 动效规范

### 渲染动画

```css
/* Markdown 符号透明度过渡 */
.md-delimiter {
  transition: opacity 150ms ease-out;
}

/* 链接悬停效果 */
.md-link {
  transition: color 150ms ease-out;
}

/* 代码块渲染 */
.md-code-block {
  animation: fadeIn 200ms ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(-2px); }
  to { opacity: 1; transform: translateY(0); }
}
```

### 菜单动画

```css
/* 更多菜单展开 */
.editor-more-menu {
  animation: menuSlideIn 150ms cubic-bezier(0, 0, 0.2, 1);
}

@keyframes menuSlideIn {
  from {
    opacity: 0;
    transform: translateY(-8px) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
```

---

## 响应式适配

### 最小尺寸支持

```
最小宽度: 300px（编辑器内容区）
  - 代码块保持可读性
  - 长链接自动换行
  - 列表保持正常缩进

最小高度: 200px
```

---

## CSS 变量汇总

```css
:root {
  /* === Markdown 专用色彩 === */
  --color-md-delimiter: rgba(102, 102, 102, 0.3);
  --color-md-code-bg: #f8f9fa;
  --color-md-code-text: #e83e8c;
  --color-md-link: #0066cc;
  --color-md-link-hover: #0052a3;
  --color-md-quote-bg: #f8f9fa;
  --color-md-quote-border: #dee2e6;
  --color-md-hr: #e5e5e5;

  /* === Markdown 尺寸 === */
  --md-h1-size: 20px;
  --md-h2-size: 16px;
  --md-h3-size: 14px;
  --md-code-size: 12px;
  --md-code-padding: 2px 6px;
  --md-code-block-padding: 12px;
  --md-quote-padding: 8px 12px;
  --md-list-indent: 18px;

  /* === Markdown 间距 === */
  --md-h1-margin-top: 16px;
  --md-h1-margin-bottom: 8px;
  --md-h2-margin-top: 12px;
  --md-h2-margin-bottom: 6px;
  --md-h3-margin-top: 8px;
  --md-h3-margin-bottom: 4px;
  --md-blockquote-margin: 8px;
  --md-hr-margin: 16px;
}
```

---

## 图标资源

### 需要的图标

| 图标 | 用途 | 风格 |
|------|------|------|
| ⋮ | 更多菜单 | 竖向三点 |
| 📋 | 复制 | 简报/剪贴板 |
| ℹ️ | 信息/提示 | 圆形信息 |
| × | 关闭 | × |

### SVG 定义

```svg
<!-- 更多菜单图标 -->
<svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
  <circle cx="8" cy="3" r="1.5"/>
  <circle cx="8" cy="8" r="1.5"/>
  <circle cx="8" cy="13" r="1.5"/>
</svg>

<!-- 复制图标 -->
<svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5">
  <rect x="3" y="5" width="8" height="10" rx="1"/>
  <path d="M5 5V3a1 1 0 0 1 1-1h6a1 1 0 0 1 1 1v10"/>
</svg>

<!-- 信息图标 -->
<svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
  <circle cx="8" cy="8" r="7" fill-opacity="0.1"/>
  <circle cx="8" cy="4" r="1"/>
  <path d="M8 7v5"/>
</svg>
```

---

## 设计交付物

- [x] UI 设计规范文档
- [ ] 可交互 HTML 原型
- [ ] CSS 样式代码
- [ ] 图标 SVG 资源

---

## 附录：视觉参考

| 产品 | 借鉴点 |
|------|--------|
| **Bear** | Markdown 符号半透明处理 |
| **Notion** | 斜杠命令菜单结构 |
| **Craft** | 标题层级视觉处理 |
| **Obsidian** | 代码块样式 |
