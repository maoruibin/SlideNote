# SlideNote Markdown 编辑器设计指南

> **版本**: v0.0.2
> **设计师**: Claude
> **创建日期**: 2025-01-13
> **状态**: 设计完成

---

## 设计文件说明

本文件夹包含 Markdown 编辑器功能的完整设计资源：

| 文件 | 说明 |
|------|------|
| `PRD.md` | 产品需求文档，定义功能范围和用户交互 |
| `UI-Spec.md` | UI 设计规范，包含样式、色彩、布局等详细定义 |
| `prototype.html` | 可交互的 HTML 原型，可直接在浏览器中预览效果 |
| `icons.svg` | 图标资源库，包含所有设计所需的 SVG 图标 |
| `Design-Guide.md` | 本文档，设计说明和开发对接指南 |

---

## 如何查看原型

### 方法一：直接打开

```bash
# 在浏览器中打开原型文件
open docs/versions/v0.0.2/features/markdown-editor/prototype.html
```

### 方法二：使用本地服务器

```bash
# 在项目根目录运行
npx serve .
# 然后访问 http://localhost:3000/docs/versions/v0.0.2/features/markdown-editor/prototype.html
```

---

## 核心设计决策

### 1. 渲染方案：样式提示

**选择理由**：
- 实现复杂度低，性能好
- 用户可以看到原始 Markdown，学习成本低
- 类似 Bear、Notion 的成熟交互模式

**视觉效果**：
- Markdown 分隔符（`**`、`*`、`` ` ``）以 25% 透明度显示
- 保持内容可编辑性
- 不破坏现有的 contenteditable 行为

### 2. 更多菜单 [⋮]

**位置**：编辑器右上角，标题输入框右侧

**菜单项**：
1. `📋 复制为 Markdown` - 复整条笔记的 Markdown 源码
2. `ℹ️ 语法快捷键提示` - 显示 Markdown 语法帮助

**交互**：
- 点击按钮展开菜单
- 点击菜单项或外部区域关闭菜单
- ESC 键关闭

### 3. 支持的 Markdown 语法

| 语法 | 示例 | 渲染效果 |
|------|------|----------|
| 标题 H1-H3 | `# 标题` | 大号加粗 |
| 粗体 | `**粗体**` | **粗体** |
| 斜体 | `*斜体*` | *斜体* |
| 行内代码 | `` `代码` `` | 灰色背景代码 |
| 代码块 | ` ```代码``` ` | 灰色背景代码块 |
| 链接 | `[文本](url)` | 蓝色下划线链接 |
| 无序列表 | `- 项目` | • 项目 |
| 有序列表 | `1. 项目` | 1. 项目 |
| 引用 | `> 引用` | 左边框+灰色背景 |
| 分割线 | `---` | 横线 |

---

## 设计 Token

### 色彩系统

```css
/* Markdown 专用色彩 */
--color-md-delimiter: rgba(102, 102, 102, 0.25);  /* 分隔符透明度 */
--color-md-code-bg: #f8f9fa;                       /* 代码背景 */
--color-md-code-text: #e83e8c;                     /* 代码文字 */
--color-md-link: #0066cc;                          /* 链接 */
--color-md-quote-bg: #f8f9fa;                      /* 引用背景 */
--color-md-quote-border: #dee2e6;                  /* 引用边框 */
```

### 尺寸规范

```css
/* Markdown 元素尺寸 */
--md-h1-size: 20px;        /* 一级标题 */
--md-h2-size: 16px;        /* 二级标题 */
--md-h3-size: 14px;        /* 三级标题 */
--md-code-size: 12px;      /* 代码字号 */
--md-list-indent: 18px;    /* 列表缩进 */
```

### 间距规范

```css
/* Markdown 元素间距 */
--md-h1-margin-top: 16px;
--md-h2-margin-top: 12px;
--md-h3-margin-top: 8px;
--md-blockquote-margin: 8px;
--md-hr-margin: 16px;
```

---

## 开发对接指南

### 文件修改清单

| 文件 | 修改内容 | 优先级 |
|------|----------|--------|
| `src/sidepanel/components/NoteEditor.js` | 替换 textarea，添加 Markdown 渲染 | P0 |
| `src/sidepanel/components/EditorMoreMenu.js` | 新建：更多菜单组件 | P0 |
| `src/sidepanel/components/SyntaxHelpModal.js` | 新建：语法提示弹窗 | P1 |
| `src/sidepanel/utils/markdown.js` | 新建：Markdown 解析工具 | P0 |
| `src/sidepanel/styles.css` | 添加 Markdown 样式 | P0 |

### 核心实现要点

1. **Markdown 解析**：使用正则表达式匹配语法，包裹对应的 CSS 类
2. **分隔符处理**：为 `**`、`*`、`` ` `` 等符号添加 `.md-delimiter` 类
3. **复制功能**：监听 `copy` 事件，根据菜单选择复制 HTML 或纯文本
4. **菜单交互**：点击外部关闭，ESC 键关闭

### CSS 类命名规范

```css
/* Markdown 元素类 */
.md-h1, .md-h2, .md-h3        /* 标题 */
.md-bold                       /* 粗体 */
.md-italic                     /* 斜体 */
.md-inline-code                /* 行内代码 */
.md-code-block                 /* 代码块 */
.md-link                       /* 链接文字 */
.md-link-url                   /* 链接 URL */
.md-delimiter                  /* 分隔符 */
.md-list-item                  /* 列表项 */
.md-blockquote                 /* 引用 */
.md-hr                         /* 分割线 */
```

---

## 视觉参考

| 产品 | 借鉴点 |
|------|--------|
| **Bear** | Markdown 符号半透明处理 |
| **Notion** | 菜单结构和动画 |
| **Craft** | 标题层级视觉处理 |
| **Obsidian** | 代码块样式 |

---

## 设计交付检查清单

- [x] 产品需求文档 (PRD.md)
- [x] UI 设计规范 (UI-Spec.md)
- [x] 可交互原型 (prototype.html)
- [x] 图标资源 (icons.svg)
- [x] 设计指南 (Design-Guide.md)
- [x] CSS 变量定义
- [x] 交互说明

---

## 变更记录

| 日期 | 版本 | 变更内容 | 负责人 |
|------|------|----------|--------|
| 2025-01-13 | v0.0.2 | 初始设计版本 | Claude |
